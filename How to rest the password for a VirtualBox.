Reset a VM user password
===============================
1. Run the VM
2. Hit Shift command 
3. Go to a recovery mod
4. Go to Root 
5. Type this command for make the files for Read and Write::   mount -rw -o remount /
6. Then use this command to change the password of any user::  passwd "UserName" 
7. Then enter the new Password and type exit
